# Waive Python AI Layer — Dependencies
# Core client has no external deps (stdlib only).
# AI skills require the packages below.

numpy==1.24.3
torch==2.0.1
torchaudio==2.0.2
demucs==4.0.1            # Stem separation (StemSplitter)
librosa==0.10.1          # Audio analysis (GrooveMatcher, GhostEngineer, TemporalArranger)
scipy==1.10.1            # Signal processing (TemporalArranger)
midiutil==1.2.1          # MIDI generation (GrooveMatcher)
soundfile==0.12.1        # Audio I/O (TemporalArranger)
pytest==7.4.3            # Testing framework
